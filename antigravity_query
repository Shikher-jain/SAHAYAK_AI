for input ingestion where is the audio option keep it here use pyttsx3 as option to speaken form answer
and speech_recognition  for SST as input durect write on input chat box in all as a input ingestion and as a query asking 

when the RAG pipeline run you have to visualize the pipeline on UI 

if backend is down or time to start or awaken then you have to tell the user that backen is starinf a while 
do not show then backend path when not running 
======
This is a large, multi-feature request. Let me research the existing codebase thoroughly before proposing a plan.

Viewed app.py:1-100
Viewed app.py:100-200
Viewed app.py:200-400
Viewed app.py:400-600
Viewed app.py:600-917

Now let me check the sahayak-ui (Vite/React) to understand what exists there:

Listed directory components
Listed directory pages
Viewed SearchChat.jsx:1-100
Viewed SearchChat.jsx:100-200
Viewed SearchChat.jsx:300-386
Viewed App.jsx:1-73
Viewed AppContext.jsx:1-100
Viewed AppContext.jsx:100-255
Viewed MainLayout.jsx:1-80

Now let me look at the Dashboard to see how connection status is currently shown:

Viewed Dashboard.jsx:1-80

Now let me check the `client.js` to see how errors are currently displayed:

Viewed client.js:1-91

I have all the context I need. Let me implement all four features now across the React UI.

Viewed Upload.jsx:1-80

Good. Now I have all the information I need. Let me implement everything:

**1. First — `BackendStatus.jsx`** — smart backend wakeup banner
**2. Update `client.js`** — hide URL in error messages  
**3. Update `SearchChat.jsx`** — add voice input/output + RAG pipeline visualization  
**4. Update `Upload.jsx`** — add audio recording tab
**5. Update `MainLayout.jsx`** — embed the backend status banner

Created BackendStatus.jsx
Created RAGPipeline.jsx

Now create the voice hook that both SearchChat and Upload will share:

Created useVoice.js

Now rewrite `SearchChat.jsx` with all new features:

Viewed SearchChat.jsx:200-300

Now I'll write the full updated `SearchChat.jsx`:

Created SearchChat.jsx

Now add backend status banner to MainLayout and audio recording tab to Upload:

Viewed MainLayout.jsx:80-180
Searched for "main className"
Viewed MainLayout.jsx:300-372
Edited MainLayout.jsx

Now add the audio recording tab to Upload.jsx:

Viewed Upload.jsx:80-365

Now I'll update Upload.jsx to add the audio recording tab:

Edited Upload.jsx

The diff shows a structural issue with the audio branch in `handleIngest`. Let me fix that:

Viewed Upload.jsx:103-115
Viewed Upload.jsx:115-155

The audio branch was inserted inside the `files` block. Let me fix the entire `handleIngest`:

Edited Upload.jsx

Good. Now also update `client.js` to not expose the raw URL in network errors:

Edited client.js

Now let's build and commit everything:

Ran command: `cd "d:\shikher sih\SAHAYAK_AI\sahayak-ui"; npm run build 2>&1 | Select-String -Pattern "error|warning|Error|built" | Select-Object -First 30`
Used tool: schedule
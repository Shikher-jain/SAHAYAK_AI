# Dev/test dependencies — not needed in production containers.
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest
pytest-asyncio
httpx